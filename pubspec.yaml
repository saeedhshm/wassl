name: wassl
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

#  [!] Flutter (Channel stable, 3.7.9, on macOS 12.6.4 21G526 darwin-x64, locale en-US)
#  • Flutter version 3.7.9 on channel stable at /Users/mac/development/3_7_9/flutter
#              • Dart version 2.19.6
#              • DevTools version 2.20.1

# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.1.15+61


environment:
  sdk: ">=2.19.6 <3.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  dropdown_button2:
    path: ../wassl/plugins/dropdown_button2-1.9.1
  flutter_svg:
  get:
#  flutter_neat_and_clean_calendar:
#    path: ../wassl/plugins/flutter_neat_and_clean_calendar-0.3.9+23
  http: ^0.13.4
  flutter_app_badger:
  shared_preferences:
  geolocator: ^9.0.2
  app_settings: ^4.2.0
  internet_connection_checker: ^1.0.0+1
  flutter_app_version_checker: ^0.3.2
  flutter_local_notifications:
#  rxdart:
  url_launcher: ^6.1.8
  file_picker: ^5.2.5
  month_year_picker:
  timezone: ^0.9.1
  device_info_plus: ^8.1.0
  local_auth: ^2.1.6
  table_calendar: ^3.0.8
  firebase_messaging: ^13.0.1
  firebase_core: ^1.24.0
  path_provider:
  flutter_pdfview: ^1.3.1
  open_file: ^3.3.2
  awesome_dialog: ^3.1.0

#  overlay_support: ^2.1.0

dependency_overrides:
  firebase_core_platform_interface: 4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.11.0"

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^1.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/images/bottom_nav/
    - assets/images/profile/
    - assets/images/placeholders/
    - assets/images/settingicons/
    - assets/app_logo/
    - assets/images/order_details/
    - assets/images/prosedures/
    - assets/images/holidays/
    - assets/images/ad_details/
    - assets/images/filters/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: AraHamah
      fonts:
        - asset: assets/fonts/arabic/jazeera/ArbFONTS-Al-Jazeera-Arabic-Bold.ttf
        - asset: assets/fonts/arabic/jazeera/ArbFONTS-Al-Jazeera-Arabic-Bold-1.ttf
        - asset: assets/fonts/arabic/jazeera/ArbFONTS-Al-Jazeera-Arabic-Light.ttf
        - asset: assets/fonts/arabic/jazeera/ArbFONTS-Al-Jazeera-Arabic-Regular.ttf

  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages


flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/app_logo/wasl_logo.png"
  min_sdk_android: 21 #