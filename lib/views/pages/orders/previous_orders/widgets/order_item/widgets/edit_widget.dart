import 'package:flutter/material.dart';

class EditOrderWidget extends StatelessWidget {
  const EditOrderWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
