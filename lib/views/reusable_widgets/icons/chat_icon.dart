import 'package:flutter/material.dart';

class ChatConversationIcon extends StatelessWidget {
  const ChatConversationIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
        'assets/images/conversation.png');
  }
}
